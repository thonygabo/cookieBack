package com.cookie.cookie.repository;

import com.cookie.cookie.entity.TipoOrdenCompra;

import org.springframework.data.jpa.repository.JpaRepository;

public interface TipoOrdenCompraRepository extends JpaRepository<TipoOrdenCompra, Integer> {
    
}
