package com.cookie.cookie.repository;

import com.cookie.cookie.entity.Pais;

import org.springframework.data.jpa.repository.JpaRepository;

public interface PaisRepository extends JpaRepository<Pais, Integer>{
    
}
